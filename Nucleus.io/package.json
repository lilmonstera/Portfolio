{
  "name": "nucleus-io-site",
  "private": true,
  "version": "1.0.0",
  "description": "Static marketing site for Nucleus.io. Tailwind CSS is compiled ahead of time into tailwind.css — run `npm run build:css` after changing any class names in the HTML files.",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.js -i tailwind.input.css -o tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.10",
    "tailwindcss": "^3.4.0"
  }
}
